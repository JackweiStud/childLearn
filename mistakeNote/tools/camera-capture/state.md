# camera-capture · 工具状态

> 本文档记录这个工具的核心诉求、功能拆解和进展。供后续开发时快速对齐上下文。

---

## 一、在 mistakeNote 中的位置

```
孩子做错题 → [拍照采集] → 识别/切题 → 归档到错题库 → 生成复习卷 → 孩子重做
              ▲
              本工具
```

camera-capture 是整条链的**第一环**——把纸上的试卷变成数字文件。它的下游是识别、裁剪、归档，最终产出物是给孩子做的复习卷。

**这个工具成功的标志**：家长拿到孩子批改完的试卷后，30 秒内完成拍照并存入系统，不需要任何额外操作。

---

## 二、核心诉求

把**纸质试卷/作业**变成**结构化的数字素材**，具体要解决：

1. **采集**——用摄像头把试卷拍下来，质量够后续识别用（清晰、不变形、曝光正常）
2. **标记**——拍照时能快速框出错题区域，省去后续人工定位（工具辅助手--框选或者选择截图）
3. **存储**——按日期自动归档，配对元数据 JSON，后续处理管线可直接消费
4. **来源多样**——USB 摄像头、iPhone（Continuity Camera）都要能用

---

## 三、功能拆解与进展

### 📷 采集能力

| 功能 | 状态 | 说明 |
|------|------|------|
| 统一摄像头下拉选择 | ✅ 完成 | WebRTC + ffmpeg/AVFoundation 统一归并在同一个下拉框中 |
| USB 摄像头选择 | ✅ 完成 | 优先选择 `USB Camera` 设备 |
| 4K / 最高分辨率优先 | ✅ 完成 | ideal 3840×2160，按设备能力降级 |
| 分辨率切换（4K/1080p/720p） | ✅ 完成 | 下拉菜单选择，原生通道时自动灰色禁用 |
| 实时预览 | ✅ 完成 | `<video>` 标签 + WebRTC |
| 手动开启/关闭摄像头 | ✅ 完成 | 默认不开，放好试卷再开 |
| 页面加载自动检测设备 | ✅ 完成 | 进入页面即触发设备枚举，无需手动刷新 |
| 刷新设备列表 | ✅ 完成 | 刷新前先停流再重扫 |
| 显示实际分辨率 | ✅ 完成 | 顶部控制栏状态文字 |
| 未授权设备智能提示 | ✅ 完成 | 未授权摄像头标注为"需点击开启授权"，引导用户触发浏览器权限 |
| iPhone Continuity Camera 唤醒引导 | ✅ 完成 | 无设备时，底部日志给出 FaceTime/Photo Booth 唤醒步骤 |
| iPhone 未检测提示卡片 | ✅ 完成 | 浏览器+ffmpeg 都没看到 iPhone 时，deviceSelect 下方显示橙色提示卡（横放/屏幕朝下/蓝牙+Wi-Fi/连续互通/不低电量） |
| 启动后 3 秒自动静默重扫 | ✅ 完成 | 解决 macOS 异步注册 iPhone 的 1-3 秒延迟。新设备出现走 toast 提示，重扫前保留用户当前选择 |

### 📱 iPhone / 原生通道

| 功能 | 状态 | 说明 |
|------|------|------|
| ffmpeg AVFoundation 设备发现 | ✅ 完成 | `/api/system-cameras` |
| 原生预览（ffmpeg → MJPEG → 前端轮询） | ✅ 完成 | session 管理 + 帧提取 |
| 原生拍照（从预览帧保存） | ✅ 完成 | |
| 原生单帧采集（ffmpeg 直拍） | ✅ 完成 | `/api/native-captures` |
| 设备诊断（浏览器 vs 系统可见对比） | ✅ 完成 | 收编到底部可折叠日志抽屉中 |

### 🖊️ 标注

| 功能 | 状态 | 说明 |
|------|------|------|
| 矩形标注 | ✅ 完成 | 支持多色，归一化坐标 |
| 圆形标注 | ✅ 完成 | |
| 自由画笔 | ✅ 完成 | |
| 橡皮擦（擦擦） | ✅ 完成 | Canvas destination-out + 离屏画布防穿透底图 |
| 撤销上一笔 | ✅ 完成 | 历史栈 pop，支持逐步回退 |
| 清空全部标注 | ✅ 完成 | |
| 画笔颜色选择器 | ✅ 完成 | 红/蓝/绿/黑/黄 五色可选 |
| 插入文字标注 | ✅ 完成 | 5 种字体（系统/宋/黑/楷/等宽）× 6 字号（16-64px）× 复用颜色，归一化坐标，画布缩放后视觉尺寸一致 |
| 区域截图（选择截图保存） | ✅ 完成 | 画框松手后自动裁剪保存，蓝色虚线 + 遮罩反馈 |
| 标注画入保存的 JPG | ✅ 完成 | 离屏画布合成，裁剪场景坐标正确映射 |
| 工具栏二次点击取消选中 | ✅ 完成 | 点已激活工具的按钮 = 清除高亮，画布暂不响应任何操作 |

### 🎛️ 画质调节

| 功能 | 状态 | 说明 |
|------|------|------|
| 硬件控制（缩放/亮度/对比度/饱和度/锐度/曝光/焦距/色温） | ✅ 完成 | 依赖 `getCapabilities()`，按设备动态渲染 |
| 硬件枚举控制（对焦/曝光/白平衡模式） | ✅ 完成 | |
| 过曝/过暗检测 | ✅ 完成 | 每 1.5 秒采样亮度 |
| 软件缩放（1×–3×） | ✅ 完成 | 中心裁剪 |
| 软件亮度/对比度/饱和度 | ✅ 完成 | CSS filter，同时影响预览和保存 |
| 硬件/软件智能切换 | ✅ 完成 | 有硬件能力优先硬件，否则降级软件兜底 |

### 💾 存储

| 功能 | 状态 | 说明 |
|------|------|------|
| 自动按日期建目录 `_inbox/scans/YYYY-MM-DD/` | ✅ 完成 | |
| JPG + JSON 配对保存 | ✅ 完成 | |
| 同秒序号防碰撞 | ✅ 完成 | `-001`, `-002`... |
| 元数据记录（时间、设备、分辨率、质量、阶段） | ✅ 完成 | `stage: 'raw_scan', status: 'unprocessed'` |
| 最近拍摄列表 | ✅ 完成 | 最多 10 条，双击调用 macOS 原生打开大图 |
| **刷新页面后保留今天图片** | ✅ 完成 | `GET /api/captures?limit=10` 扫今天目录返回按 mtime 倒序；`/scans/...` 静态路由直链原图（local-first，无客户端缓存）|
| 物理删除拍摄记录 | ✅ 完成 | 列表项「删除」按钮 = 同时删 JPG + JSON |
| 复制路径到剪贴板 | ✅ 完成 | 列表项「复制」按钮 |
| 打开保存目录（Finder） | ✅ 完成 | |
| 双击打开大图 | ✅ 完成 | 后端 `/api/open-file` 调用 macOS `open` 命令 |

### 🚧 A4 辅助

| 功能 | 状态 | 说明 |
|------|------|------|
| A4 比例参考框 | ✅ 完成 | CSS 叠加层 |
| 自动边缘检测 / 透视校正 | ❌ 未做 | 计划外，可能用 iPhone 扫描替代 |

### 🎨 UI 体验

| 功能 | 状态 | 说明 |
|------|------|------|
| 画面旋转 90° + 重置 | ✅ 完成 | 旋转后标注/截图坐标做逆变换还原到 layout 坐标系（参考 state.md 第七节踩坑） |
| 全屏取景标注 | ✅ 完成 | `requestFullscreen()`，全屏后用 rAF 等 reflow 完成再 resize canvas |
| 左右工具栏可折叠 | ✅ 完成 | 边缘 toggle 按钮，translateX 完整移出 + 暗色高亮箭头 |
| 暗色工具栏主题 | ✅ 完成 | 与黑色视频画板融洽，避免亮色卡片在视频上突兀 |
| 底部日志 fixed bottom sheet | ✅ 完成 | 展开时向上遮挡而非撑页面，避免出现垂直滚动条 |
| 键盘快捷键 | ✅ 完成 | ⌘S 拍照 / ⌘Z 撤销 / ESC 清空标注 |
| 拍照闪屏 shutter flash | ✅ 完成 | 拍照后白色快闪反馈 |

---

## 四、明确不做（当前阶段）

这些功能属于下游处理管线，不在 camera-capture 范围内：

- ✗ OCR 文字识别
- ✗ 自动识别错题
- ✗ 自动切题（从整张试卷裁出单道题）
- ✗ 自动归档到 `二年级/数学/错题/`
- ✗ 生成复习卷

---

## 五、已知问题 & 改进方向

### 短期可改进

- [x] **撤销标注**：支持撤销最近一次绘制
- [x] **橡皮擦工具**：Canvas destination-out 擦除笔迹
- [x] **多色画笔**：红/蓝/绿/黑/黄五色选择器
- [x] **区域截图自动保存**：画框松手即保存，无需额外操作
- [x] **统一摄像头选择**：WebRTC 和 ffmpeg 设备合并为单一下拉框
- [x] **未授权设备提示**：引导用户点击开启以触发浏览器权限弹窗
- [x] **双击打开大图**：最近拍摄列表双击调用系统原生图片查看器
- [x] **拍照闪屏反馈**：shutter flash 已实现
- [x] **插入文字标注**：字体/字号/颜色可选，画入保存的 JPG
- [x] **刷新页面后保留今天图片**：启动时拉 GET /api/captures 扫今天目录
- [x] **iPhone 检测改进**：自动重扫 + 未检测时提示卡片
- [x] **旋转/全屏/窗口缩放后标注坐标修正**：layout 坐标系归一化
- [ ] **拍照音效**：可选轻量反馈（视频画面 mute 时不需要）
- [ ] **批量拍摄模式**：连续拍多页试卷时减少点击

### 中期方向

- [ ] **iPhone 扫描导入**：监听 `_inbox/` 文件夹，接收 Continuity Camera Scan Documents 导入的文件
- [ ] **自动裁剪提示**：拍照后简单检测纸张边缘，提示是否需要重拍
- [ ] **缩略图预览**：保存时同时生成小图，加速后续浏览

### 与下游管线的对接点

camera-capture 的输出格式已经为下游准备好：

```
_inbox/scans/YYYY-MM-DD/YYYYMMDD-HHMMSS-usb-camera-001.jpg   ← 原始图
_inbox/scans/YYYY-MM-DD/YYYYMMDD-HHMMSS-usb-camera-001.json  ← 元数据
```

JSON 中 `status: 'unprocessed'` 表示还没被后续管线消费。下游处理完后应更新此状态。

---

## 六、技术栈与代码结构

- **运行时**：Node.js ≥ 20（纯 CommonJS，零依赖）
- **前端**：原生 HTML + JS + CSS，无框架、无构建
- **服务端**：`node:http` 手写路由
- **原生采集**：ffmpeg + AVFoundation（macOS）
- **端口**：`http://localhost:8731`

### 前端模块化结构（v2 重构后）

```
src/public/
├── index.html              # 页面结构：左工具栏 | 中预览 | 右设置+最近 | 底部日志
├── styles.css              # 全局样式：暖色调产品级 UI，响应式布局
├── ui.js                   # DOM 引用缓存、日志面板、状态提示
├── camera-handler.js       # 统一摄像头管理：WebRTC/native 切换、设备检测、画质调节、拍照
├── annotation-handler.js   # 标注工具：矩形/圆形/画笔/擦除/截图/撤销/颜色
├── recent-handler.js       # 最近拍摄列表、双击打开大图、Finder 目录打开
├── capture-effects.js      # 软件滤镜参数定义与 CSS filter 转换（UMD，Node 可测试）
└── app.js                  # 主调度器：模块初始化、事件绑定、定时检测循环
```

---

## 七、开发踩坑笔记

> 这个工具涉及 canvas 标注 + CSS transform（旋转、缩放、全屏），栽过几次坑沉淀在这里。改这块代码前先读这一节，能少走半天弯路。

### 7.1 CSS transform 后的 DOM 几何，所有"自带方法"都要重审

凡是元素带 `transform: rotate/scale`，下面这些 API 的语义跟直觉不一样：

| API | 含义 | 受 CSS transform 影响？ |
|---|---|---|
| `getBoundingClientRect()` | **视觉**包围盒——已经被 transform 后的那个矩形 | ✅ 受影响（rotate 90° 后 width/height **互换**） |
| `offsetWidth / offsetHeight` | **layout** 尺寸——元素布局时的真实宽高 | ❌ 不受影响 |
| `clientX / clientY`（事件） | 鼠标在视口里的物理像素 | ❌ 不受影响（永远是视口绝对坐标） |

**已经踩过的两个具体坑：**

1. **canvas 像素缓冲区尺寸用 `getBoundingClientRect()`** → 旋转 90° 后 buffer 宽高反了，绘制扭曲。**正解：用 `offsetWidth × dpr` 设 `canvas.width`**。
2. **点击归一化坐标用视觉宽高直接除** → 16:9 画布旋转 90° 后视觉宽高比变成 9:16，归一化值畸变。**正解：先把视觉坐标用视觉宽高做逆旋转，还原成 layout 坐标，再用 `offsetWidth/Height` 归一化**（参考点固定为「未旋转 canvas 的左上角」）。

### 7.2 全屏切换是异步 reflow

`fullscreenchange` 事件触发那一刻浏览器还没完成 layout，立刻取 `offsetWidth` 拿到的是旧值。

**正解：** 用 `requestAnimationFrame` 延迟一帧再调 resize/重绘函数。

### 7.3 ResizeObserver 要观察对的元素

之前 `recent-panel` 高度跟不上主区，因为 ResizeObserver 观察的是 `previewWrap`，漏掉了上面的控制条高度。**正解：观察包含**所有**需要同步内容的最外层容器**（这里是 `centerWorkspace`）。

### 7.4 几何/坐标改动的回归测试矩阵

凡是涉及坐标/尺寸计算的修改，**手动验证必须覆盖**：

- [ ] 旋转 0° 画一笔 → 落点正确
- [ ] 旋转 90° 画一笔 → 落点正确
- [ ] 旋转 180° 画一笔 → 落点正确
- [ ] 旋转 270° 画一笔 → 落点正确
- [ ] 进/退全屏后画一笔 → 落点正确
- [ ] 浏览器窗口拉伸后画一笔 → 落点正确

少测一个就可能埋坑。这次 90/270 度的畸变就是因为只在 0° 下肉眼"看着对"就放过了。

---

*最后更新：2026-06-28（同步本轮 session 改动：文字标注、自动重扫、iPhone 提示、刷新保留、旋转/全屏坐标修正）*
